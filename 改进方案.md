## 2026-04-10 项目可改进方案

本节用于将后续改进从“讨论结论”转为“可执行计划”，优先保障实验结果的正确性、可复现性与可维护性。

### 一、P0

1. 修复 multi_model_umap 配置读取层级问题
- 问题：CSV 分支中使用了 split.test_size / split.random_state 点路径读取，当前配置读取器不支持该访问方式。
- 影响：实际切分参数可能未按配置生效，导致对比结果偏差。
- 目标文件：scripts/multi_model_umap.py
- 验收标准：修改后 split 配置可被正确读取，日志中打印的 test_size/random_state 与配置一致。

2. 修复 multi_model_umap 的 HYBRID 构建逻辑
- 问题：HYBRID 组装时部分基模型可能因未实例化为有效 estimator 被过滤。
- 影响：UMAP 中 HYBRID 决策边界可能并不代表真实混合模型。
- 目标文件：scripts/multi_model_umap.py
- 验收标准：HYBRID 训练日志中明确包含 mlp/rf/svm/xgb 四个基学习器。

3. CNN 交叉验证改为分层抽样
- 问题：当前 KFold 未按类别分层。
- 影响：类别不平衡时各折分布波动大，验证方差增大。
- 目标文件：eeg_emotion/dl/torch/trainer.py
- 验收标准：改为 StratifiedKFold 后，每折类别分布近似整体分布。

4. LSTM 全链路随机种子统一
- 问题：LSTM 脚本存在随机打乱但缺少统一显式种子初始化入口。
- 影响：同配置重复训练波动较大，难以复现实验结论。
- 目标文件：scripts/train_lstm_dl.py
- 验收标准：同配置重复 3 次，关键指标波动显著收敛（并在日志中记录 seed）。

### 二、P1

1. XGBoost 设备选择策略优化
- 现状：默认 device=cuda。
- 风险：无 GPU 环境运行失败或需手动改配置。
- 建议：默认 auto/cpu，检测到 CUDA 再启用 GPU。
- 目标文件：scripts/train.py

2. 输出目录命名规范统一
- 现状：sklearn 与 DL 脚本对 run_name 策略不一致。
- 风险：实验管理与回溯困难。
- 建议：统一为“模型名+时间戳”或统一遵循 output.run_name 规则。
- 目标文件：scripts/train.py, scripts/train_lstm_dl.py, scripts/train_cnn_dl.py

3. verify_consistency 动态化
- 现状：脚本中存在测试集规模硬编码（81/117）。
- 风险：数据规模变化后误报。
- 建议：基于实际标签文件与 metrics 动态验证，不写死样本数。
- 目标文件：verify_consistency.py

4. 文档与代码结构对齐
- 现状：README 中仍有 TensorFlow 结构说明，与当前代码树不完全一致。
- 风险：新人上手理解成本高。
- 建议：按当前目录结构同步更新 README。
- 目标文件：README.md

### 三、P2

1. 增加最小自动化测试集
- 目标：先覆盖最容易回归的核心链路。
- 建议用例：
    - 配置读取测试（含嵌套键访问）
    - 数据切分一致性测试
    - 训练脚本 smoke test（快速小样本）

2. 指标记录标准化
- 建议在 metrics.json 增加：seed、split 参数、数据来源标识、关键预处理参数。
- 目标：支持横向公平比较和审计追踪。

3. 可视化模块职责拆分
- 现状：多个训练脚本重复调用并处理相同异常分支。
- 建议：抽象统一的可视化调度函数，降低重复代码和维护成本。

### 四、实施顺序（推荐）

1. 先做 P0-1 与 P0-2（保证 UMAP 比较结果可信）。
2. 再做 P0-3 与 P0-4（保证训练验证口径稳定可复现）。
3. 完成 P1（提高跨环境稳定性与实验管理质量）。
4. 最后推进 P2（自动化与长期维护能力建设）。
